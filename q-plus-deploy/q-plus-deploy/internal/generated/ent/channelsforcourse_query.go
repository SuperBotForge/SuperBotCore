// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"errors"
	"fmt"
	"math"
	"q+/internal/generated/ent/channelsforcourse"
	"q+/internal/generated/ent/courseinstance"
	"q+/internal/generated/ent/discordserver"
	"q+/internal/generated/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ChannelsForCourseQuery is the builder for querying ChannelsForCourse entities.
type ChannelsForCourseQuery struct {
	config
	ctx                *QueryContext
	order              []channelsforcourse.OrderOption
	inters             []Interceptor
	predicates         []predicate.ChannelsForCourse
	withDiscordServer  *DiscordServerQuery
	withCourseInstance *CourseInstanceQuery
	withFKs            bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ChannelsForCourseQuery builder.
func (cfcq *ChannelsForCourseQuery) Where(ps ...predicate.ChannelsForCourse) *ChannelsForCourseQuery {
	cfcq.predicates = append(cfcq.predicates, ps...)
	return cfcq
}

// Limit the number of records to be returned by this query.
func (cfcq *ChannelsForCourseQuery) Limit(limit int) *ChannelsForCourseQuery {
	cfcq.ctx.Limit = &limit
	return cfcq
}

// Offset to start from.
func (cfcq *ChannelsForCourseQuery) Offset(offset int) *ChannelsForCourseQuery {
	cfcq.ctx.Offset = &offset
	return cfcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cfcq *ChannelsForCourseQuery) Unique(unique bool) *ChannelsForCourseQuery {
	cfcq.ctx.Unique = &unique
	return cfcq
}

// Order specifies how the records should be ordered.
func (cfcq *ChannelsForCourseQuery) Order(o ...channelsforcourse.OrderOption) *ChannelsForCourseQuery {
	cfcq.order = append(cfcq.order, o...)
	return cfcq
}

// QueryDiscordServer chains the current query on the "discord_server" edge.
func (cfcq *ChannelsForCourseQuery) QueryDiscordServer() *DiscordServerQuery {
	query := (&DiscordServerClient{config: cfcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cfcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cfcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(channelsforcourse.Table, channelsforcourse.FieldID, selector),
			sqlgraph.To(discordserver.Table, discordserver.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, channelsforcourse.DiscordServerTable, channelsforcourse.DiscordServerColumn),
		)
		fromU = sqlgraph.SetNeighbors(cfcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCourseInstance chains the current query on the "course_instance" edge.
func (cfcq *ChannelsForCourseQuery) QueryCourseInstance() *CourseInstanceQuery {
	query := (&CourseInstanceClient{config: cfcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cfcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cfcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(channelsforcourse.Table, channelsforcourse.FieldID, selector),
			sqlgraph.To(courseinstance.Table, courseinstance.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, channelsforcourse.CourseInstanceTable, channelsforcourse.CourseInstanceColumn),
		)
		fromU = sqlgraph.SetNeighbors(cfcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ChannelsForCourse entity from the query.
// Returns a *NotFoundError when no ChannelsForCourse was found.
func (cfcq *ChannelsForCourseQuery) First(ctx context.Context) (*ChannelsForCourse, error) {
	nodes, err := cfcq.Limit(1).All(setContextOp(ctx, cfcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{channelsforcourse.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cfcq *ChannelsForCourseQuery) FirstX(ctx context.Context) *ChannelsForCourse {
	node, err := cfcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ChannelsForCourse ID from the query.
// Returns a *NotFoundError when no ChannelsForCourse ID was found.
func (cfcq *ChannelsForCourseQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = cfcq.Limit(1).IDs(setContextOp(ctx, cfcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{channelsforcourse.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cfcq *ChannelsForCourseQuery) FirstIDX(ctx context.Context) int64 {
	id, err := cfcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ChannelsForCourse entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ChannelsForCourse entity is found.
// Returns a *NotFoundError when no ChannelsForCourse entities are found.
func (cfcq *ChannelsForCourseQuery) Only(ctx context.Context) (*ChannelsForCourse, error) {
	nodes, err := cfcq.Limit(2).All(setContextOp(ctx, cfcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{channelsforcourse.Label}
	default:
		return nil, &NotSingularError{channelsforcourse.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cfcq *ChannelsForCourseQuery) OnlyX(ctx context.Context) *ChannelsForCourse {
	node, err := cfcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ChannelsForCourse ID in the query.
// Returns a *NotSingularError when more than one ChannelsForCourse ID is found.
// Returns a *NotFoundError when no entities are found.
func (cfcq *ChannelsForCourseQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = cfcq.Limit(2).IDs(setContextOp(ctx, cfcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{channelsforcourse.Label}
	default:
		err = &NotSingularError{channelsforcourse.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cfcq *ChannelsForCourseQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := cfcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ChannelsForCourses.
func (cfcq *ChannelsForCourseQuery) All(ctx context.Context) ([]*ChannelsForCourse, error) {
	ctx = setContextOp(ctx, cfcq.ctx, ent.OpQueryAll)
	if err := cfcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ChannelsForCourse, *ChannelsForCourseQuery]()
	return withInterceptors[[]*ChannelsForCourse](ctx, cfcq, qr, cfcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cfcq *ChannelsForCourseQuery) AllX(ctx context.Context) []*ChannelsForCourse {
	nodes, err := cfcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ChannelsForCourse IDs.
func (cfcq *ChannelsForCourseQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if cfcq.ctx.Unique == nil && cfcq.path != nil {
		cfcq.Unique(true)
	}
	ctx = setContextOp(ctx, cfcq.ctx, ent.OpQueryIDs)
	if err = cfcq.Select(channelsforcourse.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cfcq *ChannelsForCourseQuery) IDsX(ctx context.Context) []int64 {
	ids, err := cfcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cfcq *ChannelsForCourseQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cfcq.ctx, ent.OpQueryCount)
	if err := cfcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cfcq, querierCount[*ChannelsForCourseQuery](), cfcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cfcq *ChannelsForCourseQuery) CountX(ctx context.Context) int {
	count, err := cfcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cfcq *ChannelsForCourseQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cfcq.ctx, ent.OpQueryExist)
	switch _, err := cfcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cfcq *ChannelsForCourseQuery) ExistX(ctx context.Context) bool {
	exist, err := cfcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ChannelsForCourseQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cfcq *ChannelsForCourseQuery) Clone() *ChannelsForCourseQuery {
	if cfcq == nil {
		return nil
	}
	return &ChannelsForCourseQuery{
		config:             cfcq.config,
		ctx:                cfcq.ctx.Clone(),
		order:              append([]channelsforcourse.OrderOption{}, cfcq.order...),
		inters:             append([]Interceptor{}, cfcq.inters...),
		predicates:         append([]predicate.ChannelsForCourse{}, cfcq.predicates...),
		withDiscordServer:  cfcq.withDiscordServer.Clone(),
		withCourseInstance: cfcq.withCourseInstance.Clone(),
		// clone intermediate query.
		sql:  cfcq.sql.Clone(),
		path: cfcq.path,
	}
}

// WithDiscordServer tells the query-builder to eager-load the nodes that are connected to
// the "discord_server" edge. The optional arguments are used to configure the query builder of the edge.
func (cfcq *ChannelsForCourseQuery) WithDiscordServer(opts ...func(*DiscordServerQuery)) *ChannelsForCourseQuery {
	query := (&DiscordServerClient{config: cfcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cfcq.withDiscordServer = query
	return cfcq
}

// WithCourseInstance tells the query-builder to eager-load the nodes that are connected to
// the "course_instance" edge. The optional arguments are used to configure the query builder of the edge.
func (cfcq *ChannelsForCourseQuery) WithCourseInstance(opts ...func(*CourseInstanceQuery)) *ChannelsForCourseQuery {
	query := (&CourseInstanceClient{config: cfcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cfcq.withCourseInstance = query
	return cfcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ChannelsForCourse.Query().
//		GroupBy(channelsforcourse.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cfcq *ChannelsForCourseQuery) GroupBy(field string, fields ...string) *ChannelsForCourseGroupBy {
	cfcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ChannelsForCourseGroupBy{build: cfcq}
	grbuild.flds = &cfcq.ctx.Fields
	grbuild.label = channelsforcourse.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ChannelsForCourse.Query().
//		Select(channelsforcourse.FieldCreatedAt).
//		Scan(ctx, &v)
func (cfcq *ChannelsForCourseQuery) Select(fields ...string) *ChannelsForCourseSelect {
	cfcq.ctx.Fields = append(cfcq.ctx.Fields, fields...)
	sbuild := &ChannelsForCourseSelect{ChannelsForCourseQuery: cfcq}
	sbuild.label = channelsforcourse.Label
	sbuild.flds, sbuild.scan = &cfcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ChannelsForCourseSelect configured with the given aggregations.
func (cfcq *ChannelsForCourseQuery) Aggregate(fns ...AggregateFunc) *ChannelsForCourseSelect {
	return cfcq.Select().Aggregate(fns...)
}

func (cfcq *ChannelsForCourseQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cfcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cfcq); err != nil {
				return err
			}
		}
	}
	for _, f := range cfcq.ctx.Fields {
		if !channelsforcourse.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cfcq.path != nil {
		prev, err := cfcq.path(ctx)
		if err != nil {
			return err
		}
		cfcq.sql = prev
	}
	if channelsforcourse.Policy == nil {
		return errors.New("ent: uninitialized channelsforcourse.Policy (forgotten import ent/runtime?)")
	}
	if err := channelsforcourse.Policy.EvalQuery(ctx, cfcq); err != nil {
		return err
	}
	return nil
}

func (cfcq *ChannelsForCourseQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ChannelsForCourse, error) {
	var (
		nodes       = []*ChannelsForCourse{}
		withFKs     = cfcq.withFKs
		_spec       = cfcq.querySpec()
		loadedTypes = [2]bool{
			cfcq.withDiscordServer != nil,
			cfcq.withCourseInstance != nil,
		}
	)
	if cfcq.withDiscordServer != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, channelsforcourse.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ChannelsForCourse).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ChannelsForCourse{config: cfcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cfcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cfcq.withDiscordServer; query != nil {
		if err := cfcq.loadDiscordServer(ctx, query, nodes, nil,
			func(n *ChannelsForCourse, e *DiscordServer) { n.Edges.DiscordServer = e }); err != nil {
			return nil, err
		}
	}
	if query := cfcq.withCourseInstance; query != nil {
		if err := cfcq.loadCourseInstance(ctx, query, nodes, nil,
			func(n *ChannelsForCourse, e *CourseInstance) { n.Edges.CourseInstance = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cfcq *ChannelsForCourseQuery) loadDiscordServer(ctx context.Context, query *DiscordServerQuery, nodes []*ChannelsForCourse, init func(*ChannelsForCourse), assign func(*ChannelsForCourse, *DiscordServer)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ChannelsForCourse)
	for i := range nodes {
		if nodes[i].channels_for_course_discord_server == nil {
			continue
		}
		fk := *nodes[i].channels_for_course_discord_server
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(discordserver.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "channels_for_course_discord_server" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (cfcq *ChannelsForCourseQuery) loadCourseInstance(ctx context.Context, query *CourseInstanceQuery, nodes []*ChannelsForCourse, init func(*ChannelsForCourse), assign func(*ChannelsForCourse, *CourseInstance)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*ChannelsForCourse)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.CourseInstance(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(channelsforcourse.CourseInstanceColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.channels_for_course_course_instance
		if fk == nil {
			return fmt.Errorf(`foreign-key "channels_for_course_course_instance" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "channels_for_course_course_instance" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (cfcq *ChannelsForCourseQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cfcq.querySpec()
	_spec.Node.Columns = cfcq.ctx.Fields
	if len(cfcq.ctx.Fields) > 0 {
		_spec.Unique = cfcq.ctx.Unique != nil && *cfcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cfcq.driver, _spec)
}

func (cfcq *ChannelsForCourseQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(channelsforcourse.Table, channelsforcourse.Columns, sqlgraph.NewFieldSpec(channelsforcourse.FieldID, field.TypeInt64))
	_spec.From = cfcq.sql
	if unique := cfcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cfcq.path != nil {
		_spec.Unique = true
	}
	if fields := cfcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, channelsforcourse.FieldID)
		for i := range fields {
			if fields[i] != channelsforcourse.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cfcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cfcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cfcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cfcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cfcq *ChannelsForCourseQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cfcq.driver.Dialect())
	t1 := builder.Table(channelsforcourse.Table)
	columns := cfcq.ctx.Fields
	if len(columns) == 0 {
		columns = channelsforcourse.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cfcq.sql != nil {
		selector = cfcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cfcq.ctx.Unique != nil && *cfcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cfcq.predicates {
		p(selector)
	}
	for _, p := range cfcq.order {
		p(selector)
	}
	if offset := cfcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cfcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ChannelsForCourseGroupBy is the group-by builder for ChannelsForCourse entities.
type ChannelsForCourseGroupBy struct {
	selector
	build *ChannelsForCourseQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cfcgb *ChannelsForCourseGroupBy) Aggregate(fns ...AggregateFunc) *ChannelsForCourseGroupBy {
	cfcgb.fns = append(cfcgb.fns, fns...)
	return cfcgb
}

// Scan applies the selector query and scans the result into the given value.
func (cfcgb *ChannelsForCourseGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cfcgb.build.ctx, ent.OpQueryGroupBy)
	if err := cfcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ChannelsForCourseQuery, *ChannelsForCourseGroupBy](ctx, cfcgb.build, cfcgb, cfcgb.build.inters, v)
}

func (cfcgb *ChannelsForCourseGroupBy) sqlScan(ctx context.Context, root *ChannelsForCourseQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cfcgb.fns))
	for _, fn := range cfcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cfcgb.flds)+len(cfcgb.fns))
		for _, f := range *cfcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cfcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ChannelsForCourseSelect is the builder for selecting fields of ChannelsForCourse entities.
type ChannelsForCourseSelect struct {
	*ChannelsForCourseQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cfcs *ChannelsForCourseSelect) Aggregate(fns ...AggregateFunc) *ChannelsForCourseSelect {
	cfcs.fns = append(cfcs.fns, fns...)
	return cfcs
}

// Scan applies the selector query and scans the result into the given value.
func (cfcs *ChannelsForCourseSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cfcs.ctx, ent.OpQuerySelect)
	if err := cfcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ChannelsForCourseQuery, *ChannelsForCourseSelect](ctx, cfcs.ChannelsForCourseQuery, cfcs, cfcs.inters, v)
}

func (cfcs *ChannelsForCourseSelect) sqlScan(ctx context.Context, root *ChannelsForCourseQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cfcs.fns))
	for _, fn := range cfcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cfcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
