// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"q+/internal/generated/ent/examiner"
	"q+/internal/generated/ent/queue"
	"q+/internal/generated/ent/queueplace"
	"q+/internal/generated/ent/user"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Examiner is the model entity for the Examiner schema.
type Examiner struct {
	config `json:"-"`
	// ID of the ent.
	ID int64 `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Note holds the value of the "note" field.
	Note string `json:"note,omitempty"`
	// QueueID holds the value of the "queue_id" field.
	QueueID int64 `json:"queue_id,omitempty"`
	// TeacherID holds the value of the "teacher_id" field.
	TeacherID int64 `json:"teacher_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ExaminerQuery when eager-loading is set.
	Edges                        ExaminerEdges `json:"edges"`
	examiner_current_queue_place *int64
	selectValues                 sql.SelectValues
}

// ExaminerEdges holds the relations/edges for other nodes in the graph.
type ExaminerEdges struct {
	// Queue holds the value of the queue edge.
	Queue *Queue `json:"queue,omitempty"`
	// Teacher holds the value of the teacher edge.
	Teacher *User `json:"teacher,omitempty"`
	// Criteria holds the value of the criteria edge.
	Criteria []*Criterion `json:"criteria,omitempty"`
	// CurrentQueuePlace holds the value of the current_queue_place edge.
	CurrentQueuePlace *QueuePlace `json:"current_queue_place,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// QueueOrErr returns the Queue value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ExaminerEdges) QueueOrErr() (*Queue, error) {
	if e.Queue != nil {
		return e.Queue, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: queue.Label}
	}
	return nil, &NotLoadedError{edge: "queue"}
}

// TeacherOrErr returns the Teacher value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ExaminerEdges) TeacherOrErr() (*User, error) {
	if e.Teacher != nil {
		return e.Teacher, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "teacher"}
}

// CriteriaOrErr returns the Criteria value or an error if the edge
// was not loaded in eager-loading.
func (e ExaminerEdges) CriteriaOrErr() ([]*Criterion, error) {
	if e.loadedTypes[2] {
		return e.Criteria, nil
	}
	return nil, &NotLoadedError{edge: "criteria"}
}

// CurrentQueuePlaceOrErr returns the CurrentQueuePlace value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ExaminerEdges) CurrentQueuePlaceOrErr() (*QueuePlace, error) {
	if e.CurrentQueuePlace != nil {
		return e.CurrentQueuePlace, nil
	} else if e.loadedTypes[3] {
		return nil, &NotFoundError{label: queueplace.Label}
	}
	return nil, &NotLoadedError{edge: "current_queue_place"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Examiner) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case examiner.FieldID, examiner.FieldQueueID, examiner.FieldTeacherID:
			values[i] = new(sql.NullInt64)
		case examiner.FieldNote:
			values[i] = new(sql.NullString)
		case examiner.FieldCreatedAt, examiner.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case examiner.ForeignKeys[0]: // examiner_current_queue_place
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Examiner fields.
func (e *Examiner) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case examiner.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			e.ID = int64(value.Int64)
		case examiner.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				e.CreatedAt = value.Time
			}
		case examiner.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				e.UpdatedAt = value.Time
			}
		case examiner.FieldNote:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field note", values[i])
			} else if value.Valid {
				e.Note = value.String
			}
		case examiner.FieldQueueID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field queue_id", values[i])
			} else if value.Valid {
				e.QueueID = value.Int64
			}
		case examiner.FieldTeacherID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field teacher_id", values[i])
			} else if value.Valid {
				e.TeacherID = value.Int64
			}
		case examiner.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field examiner_current_queue_place", value)
			} else if value.Valid {
				e.examiner_current_queue_place = new(int64)
				*e.examiner_current_queue_place = int64(value.Int64)
			}
		default:
			e.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Examiner.
// This includes values selected through modifiers, order, etc.
func (e *Examiner) Value(name string) (ent.Value, error) {
	return e.selectValues.Get(name)
}

// QueryQueue queries the "queue" edge of the Examiner entity.
func (e *Examiner) QueryQueue() *QueueQuery {
	return NewExaminerClient(e.config).QueryQueue(e)
}

// QueryTeacher queries the "teacher" edge of the Examiner entity.
func (e *Examiner) QueryTeacher() *UserQuery {
	return NewExaminerClient(e.config).QueryTeacher(e)
}

// QueryCriteria queries the "criteria" edge of the Examiner entity.
func (e *Examiner) QueryCriteria() *CriterionQuery {
	return NewExaminerClient(e.config).QueryCriteria(e)
}

// QueryCurrentQueuePlace queries the "current_queue_place" edge of the Examiner entity.
func (e *Examiner) QueryCurrentQueuePlace() *QueuePlaceQuery {
	return NewExaminerClient(e.config).QueryCurrentQueuePlace(e)
}

// Update returns a builder for updating this Examiner.
// Note that you need to call Examiner.Unwrap() before calling this method if this Examiner
// was returned from a transaction, and the transaction was committed or rolled back.
func (e *Examiner) Update() *ExaminerUpdateOne {
	return NewExaminerClient(e.config).UpdateOne(e)
}

// Unwrap unwraps the Examiner entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (e *Examiner) Unwrap() *Examiner {
	_tx, ok := e.config.driver.(*txDriver)
	if !ok {
		panic("ent: Examiner is not a transactional entity")
	}
	e.config.driver = _tx.drv
	return e
}

// String implements the fmt.Stringer.
func (e *Examiner) String() string {
	var builder strings.Builder
	builder.WriteString("Examiner(")
	builder.WriteString(fmt.Sprintf("id=%v, ", e.ID))
	builder.WriteString("created_at=")
	builder.WriteString(e.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(e.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("note=")
	builder.WriteString(e.Note)
	builder.WriteString(", ")
	builder.WriteString("queue_id=")
	builder.WriteString(fmt.Sprintf("%v", e.QueueID))
	builder.WriteString(", ")
	builder.WriteString("teacher_id=")
	builder.WriteString(fmt.Sprintf("%v", e.TeacherID))
	builder.WriteByte(')')
	return builder.String()
}

// Examiners is a parsable slice of Examiner.
type Examiners []*Examiner
