// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"q+/internal/generated/ent/discordrole"
	"q+/internal/generated/ent/discordserver"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// DiscordRole is the model entity for the DiscordRole schema.
type DiscordRole struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Type holds the value of the "type" field.
	Type discordrole.Type `json:"type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DiscordRoleQuery when eager-loading is set.
	Edges                        DiscordRoleEdges `json:"edges"`
	discord_server_discord_roles *string
	selectValues                 sql.SelectValues
}

// DiscordRoleEdges holds the relations/edges for other nodes in the graph.
type DiscordRoleEdges struct {
	// DiscordServer holds the value of the discord_server edge.
	DiscordServer *DiscordServer `json:"discord_server,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// DiscordServerOrErr returns the DiscordServer value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DiscordRoleEdges) DiscordServerOrErr() (*DiscordServer, error) {
	if e.DiscordServer != nil {
		return e.DiscordServer, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: discordserver.Label}
	}
	return nil, &NotLoadedError{edge: "discord_server"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DiscordRole) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case discordrole.FieldID, discordrole.FieldType:
			values[i] = new(sql.NullString)
		case discordrole.ForeignKeys[0]: // discord_server_discord_roles
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DiscordRole fields.
func (dr *DiscordRole) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case discordrole.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				dr.ID = value.String
			}
		case discordrole.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				dr.Type = discordrole.Type(value.String)
			}
		case discordrole.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field discord_server_discord_roles", values[i])
			} else if value.Valid {
				dr.discord_server_discord_roles = new(string)
				*dr.discord_server_discord_roles = value.String
			}
		default:
			dr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DiscordRole.
// This includes values selected through modifiers, order, etc.
func (dr *DiscordRole) Value(name string) (ent.Value, error) {
	return dr.selectValues.Get(name)
}

// QueryDiscordServer queries the "discord_server" edge of the DiscordRole entity.
func (dr *DiscordRole) QueryDiscordServer() *DiscordServerQuery {
	return NewDiscordRoleClient(dr.config).QueryDiscordServer(dr)
}

// Update returns a builder for updating this DiscordRole.
// Note that you need to call DiscordRole.Unwrap() before calling this method if this DiscordRole
// was returned from a transaction, and the transaction was committed or rolled back.
func (dr *DiscordRole) Update() *DiscordRoleUpdateOne {
	return NewDiscordRoleClient(dr.config).UpdateOne(dr)
}

// Unwrap unwraps the DiscordRole entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (dr *DiscordRole) Unwrap() *DiscordRole {
	_tx, ok := dr.config.driver.(*txDriver)
	if !ok {
		panic("ent: DiscordRole is not a transactional entity")
	}
	dr.config.driver = _tx.drv
	return dr
}

// String implements the fmt.Stringer.
func (dr *DiscordRole) String() string {
	var builder strings.Builder
	builder.WriteString("DiscordRole(")
	builder.WriteString(fmt.Sprintf("id=%v, ", dr.ID))
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", dr.Type))
	builder.WriteByte(')')
	return builder.String()
}

// DiscordRoles is a parsable slice of DiscordRole.
type DiscordRoles []*DiscordRole
