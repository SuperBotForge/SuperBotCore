// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"q+/internal/generated/ent/criterion"
	"q+/internal/generated/ent/predicate"
	"q+/internal/generated/ent/queueplace"
	"q+/internal/generated/ent/queueplacecriterion"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
)

// QueuePlaceCriterionQuery is the builder for querying QueuePlaceCriterion entities.
type QueuePlaceCriterionQuery struct {
	config
	ctx            *QueryContext
	order          []queueplacecriterion.OrderOption
	inters         []Interceptor
	predicates     []predicate.QueuePlaceCriterion
	withQueuePlace *QueuePlaceQuery
	withCriterion  *CriterionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the QueuePlaceCriterionQuery builder.
func (qpcq *QueuePlaceCriterionQuery) Where(ps ...predicate.QueuePlaceCriterion) *QueuePlaceCriterionQuery {
	qpcq.predicates = append(qpcq.predicates, ps...)
	return qpcq
}

// Limit the number of records to be returned by this query.
func (qpcq *QueuePlaceCriterionQuery) Limit(limit int) *QueuePlaceCriterionQuery {
	qpcq.ctx.Limit = &limit
	return qpcq
}

// Offset to start from.
func (qpcq *QueuePlaceCriterionQuery) Offset(offset int) *QueuePlaceCriterionQuery {
	qpcq.ctx.Offset = &offset
	return qpcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (qpcq *QueuePlaceCriterionQuery) Unique(unique bool) *QueuePlaceCriterionQuery {
	qpcq.ctx.Unique = &unique
	return qpcq
}

// Order specifies how the records should be ordered.
func (qpcq *QueuePlaceCriterionQuery) Order(o ...queueplacecriterion.OrderOption) *QueuePlaceCriterionQuery {
	qpcq.order = append(qpcq.order, o...)
	return qpcq
}

// QueryQueuePlace chains the current query on the "queue_place" edge.
func (qpcq *QueuePlaceCriterionQuery) QueryQueuePlace() *QueuePlaceQuery {
	query := (&QueuePlaceClient{config: qpcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := qpcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := qpcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(queueplacecriterion.Table, queueplacecriterion.QueuePlaceColumn, selector),
			sqlgraph.To(queueplace.Table, queueplace.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, queueplacecriterion.QueuePlaceTable, queueplacecriterion.QueuePlaceColumn),
		)
		fromU = sqlgraph.SetNeighbors(qpcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCriterion chains the current query on the "criterion" edge.
func (qpcq *QueuePlaceCriterionQuery) QueryCriterion() *CriterionQuery {
	query := (&CriterionClient{config: qpcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := qpcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := qpcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(queueplacecriterion.Table, queueplacecriterion.CriterionColumn, selector),
			sqlgraph.To(criterion.Table, criterion.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, queueplacecriterion.CriterionTable, queueplacecriterion.CriterionColumn),
		)
		fromU = sqlgraph.SetNeighbors(qpcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first QueuePlaceCriterion entity from the query.
// Returns a *NotFoundError when no QueuePlaceCriterion was found.
func (qpcq *QueuePlaceCriterionQuery) First(ctx context.Context) (*QueuePlaceCriterion, error) {
	nodes, err := qpcq.Limit(1).All(setContextOp(ctx, qpcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{queueplacecriterion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (qpcq *QueuePlaceCriterionQuery) FirstX(ctx context.Context) *QueuePlaceCriterion {
	node, err := qpcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// Only returns a single QueuePlaceCriterion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one QueuePlaceCriterion entity is found.
// Returns a *NotFoundError when no QueuePlaceCriterion entities are found.
func (qpcq *QueuePlaceCriterionQuery) Only(ctx context.Context) (*QueuePlaceCriterion, error) {
	nodes, err := qpcq.Limit(2).All(setContextOp(ctx, qpcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{queueplacecriterion.Label}
	default:
		return nil, &NotSingularError{queueplacecriterion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (qpcq *QueuePlaceCriterionQuery) OnlyX(ctx context.Context) *QueuePlaceCriterion {
	node, err := qpcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// All executes the query and returns a list of QueuePlaceCriterions.
func (qpcq *QueuePlaceCriterionQuery) All(ctx context.Context) ([]*QueuePlaceCriterion, error) {
	ctx = setContextOp(ctx, qpcq.ctx, ent.OpQueryAll)
	if err := qpcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*QueuePlaceCriterion, *QueuePlaceCriterionQuery]()
	return withInterceptors[[]*QueuePlaceCriterion](ctx, qpcq, qr, qpcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (qpcq *QueuePlaceCriterionQuery) AllX(ctx context.Context) []*QueuePlaceCriterion {
	nodes, err := qpcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// Count returns the count of the given query.
func (qpcq *QueuePlaceCriterionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, qpcq.ctx, ent.OpQueryCount)
	if err := qpcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, qpcq, querierCount[*QueuePlaceCriterionQuery](), qpcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (qpcq *QueuePlaceCriterionQuery) CountX(ctx context.Context) int {
	count, err := qpcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (qpcq *QueuePlaceCriterionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, qpcq.ctx, ent.OpQueryExist)
	switch _, err := qpcq.First(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (qpcq *QueuePlaceCriterionQuery) ExistX(ctx context.Context) bool {
	exist, err := qpcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the QueuePlaceCriterionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (qpcq *QueuePlaceCriterionQuery) Clone() *QueuePlaceCriterionQuery {
	if qpcq == nil {
		return nil
	}
	return &QueuePlaceCriterionQuery{
		config:         qpcq.config,
		ctx:            qpcq.ctx.Clone(),
		order:          append([]queueplacecriterion.OrderOption{}, qpcq.order...),
		inters:         append([]Interceptor{}, qpcq.inters...),
		predicates:     append([]predicate.QueuePlaceCriterion{}, qpcq.predicates...),
		withQueuePlace: qpcq.withQueuePlace.Clone(),
		withCriterion:  qpcq.withCriterion.Clone(),
		// clone intermediate query.
		sql:  qpcq.sql.Clone(),
		path: qpcq.path,
	}
}

// WithQueuePlace tells the query-builder to eager-load the nodes that are connected to
// the "queue_place" edge. The optional arguments are used to configure the query builder of the edge.
func (qpcq *QueuePlaceCriterionQuery) WithQueuePlace(opts ...func(*QueuePlaceQuery)) *QueuePlaceCriterionQuery {
	query := (&QueuePlaceClient{config: qpcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	qpcq.withQueuePlace = query
	return qpcq
}

// WithCriterion tells the query-builder to eager-load the nodes that are connected to
// the "criterion" edge. The optional arguments are used to configure the query builder of the edge.
func (qpcq *QueuePlaceCriterionQuery) WithCriterion(opts ...func(*CriterionQuery)) *QueuePlaceCriterionQuery {
	query := (&CriterionClient{config: qpcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	qpcq.withCriterion = query
	return qpcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		QueuePlaceID int64 `json:"queue_place_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.QueuePlaceCriterion.Query().
//		GroupBy(queueplacecriterion.FieldQueuePlaceID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (qpcq *QueuePlaceCriterionQuery) GroupBy(field string, fields ...string) *QueuePlaceCriterionGroupBy {
	qpcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &QueuePlaceCriterionGroupBy{build: qpcq}
	grbuild.flds = &qpcq.ctx.Fields
	grbuild.label = queueplacecriterion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		QueuePlaceID int64 `json:"queue_place_id,omitempty"`
//	}
//
//	client.QueuePlaceCriterion.Query().
//		Select(queueplacecriterion.FieldQueuePlaceID).
//		Scan(ctx, &v)
func (qpcq *QueuePlaceCriterionQuery) Select(fields ...string) *QueuePlaceCriterionSelect {
	qpcq.ctx.Fields = append(qpcq.ctx.Fields, fields...)
	sbuild := &QueuePlaceCriterionSelect{QueuePlaceCriterionQuery: qpcq}
	sbuild.label = queueplacecriterion.Label
	sbuild.flds, sbuild.scan = &qpcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a QueuePlaceCriterionSelect configured with the given aggregations.
func (qpcq *QueuePlaceCriterionQuery) Aggregate(fns ...AggregateFunc) *QueuePlaceCriterionSelect {
	return qpcq.Select().Aggregate(fns...)
}

func (qpcq *QueuePlaceCriterionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range qpcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, qpcq); err != nil {
				return err
			}
		}
	}
	for _, f := range qpcq.ctx.Fields {
		if !queueplacecriterion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if qpcq.path != nil {
		prev, err := qpcq.path(ctx)
		if err != nil {
			return err
		}
		qpcq.sql = prev
	}
	return nil
}

func (qpcq *QueuePlaceCriterionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*QueuePlaceCriterion, error) {
	var (
		nodes       = []*QueuePlaceCriterion{}
		_spec       = qpcq.querySpec()
		loadedTypes = [2]bool{
			qpcq.withQueuePlace != nil,
			qpcq.withCriterion != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*QueuePlaceCriterion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &QueuePlaceCriterion{config: qpcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, qpcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := qpcq.withQueuePlace; query != nil {
		if err := qpcq.loadQueuePlace(ctx, query, nodes, nil,
			func(n *QueuePlaceCriterion, e *QueuePlace) { n.Edges.QueuePlace = e }); err != nil {
			return nil, err
		}
	}
	if query := qpcq.withCriterion; query != nil {
		if err := qpcq.loadCriterion(ctx, query, nodes, nil,
			func(n *QueuePlaceCriterion, e *Criterion) { n.Edges.Criterion = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (qpcq *QueuePlaceCriterionQuery) loadQueuePlace(ctx context.Context, query *QueuePlaceQuery, nodes []*QueuePlaceCriterion, init func(*QueuePlaceCriterion), assign func(*QueuePlaceCriterion, *QueuePlace)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*QueuePlaceCriterion)
	for i := range nodes {
		fk := nodes[i].QueuePlaceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(queueplace.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "queue_place_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (qpcq *QueuePlaceCriterionQuery) loadCriterion(ctx context.Context, query *CriterionQuery, nodes []*QueuePlaceCriterion, init func(*QueuePlaceCriterion), assign func(*QueuePlaceCriterion, *Criterion)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*QueuePlaceCriterion)
	for i := range nodes {
		fk := nodes[i].CriterionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(criterion.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "criterion_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (qpcq *QueuePlaceCriterionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := qpcq.querySpec()
	_spec.Unique = false
	_spec.Node.Columns = nil
	return sqlgraph.CountNodes(ctx, qpcq.driver, _spec)
}

func (qpcq *QueuePlaceCriterionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(queueplacecriterion.Table, queueplacecriterion.Columns, nil)
	_spec.From = qpcq.sql
	if unique := qpcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if qpcq.path != nil {
		_spec.Unique = true
	}
	if fields := qpcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		for i := range fields {
			_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
		}
		if qpcq.withQueuePlace != nil {
			_spec.Node.AddColumnOnce(queueplacecriterion.FieldQueuePlaceID)
		}
		if qpcq.withCriterion != nil {
			_spec.Node.AddColumnOnce(queueplacecriterion.FieldCriterionID)
		}
	}
	if ps := qpcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := qpcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := qpcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := qpcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (qpcq *QueuePlaceCriterionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(qpcq.driver.Dialect())
	t1 := builder.Table(queueplacecriterion.Table)
	columns := qpcq.ctx.Fields
	if len(columns) == 0 {
		columns = queueplacecriterion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if qpcq.sql != nil {
		selector = qpcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if qpcq.ctx.Unique != nil && *qpcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range qpcq.predicates {
		p(selector)
	}
	for _, p := range qpcq.order {
		p(selector)
	}
	if offset := qpcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := qpcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// QueuePlaceCriterionGroupBy is the group-by builder for QueuePlaceCriterion entities.
type QueuePlaceCriterionGroupBy struct {
	selector
	build *QueuePlaceCriterionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (qpcgb *QueuePlaceCriterionGroupBy) Aggregate(fns ...AggregateFunc) *QueuePlaceCriterionGroupBy {
	qpcgb.fns = append(qpcgb.fns, fns...)
	return qpcgb
}

// Scan applies the selector query and scans the result into the given value.
func (qpcgb *QueuePlaceCriterionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, qpcgb.build.ctx, ent.OpQueryGroupBy)
	if err := qpcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*QueuePlaceCriterionQuery, *QueuePlaceCriterionGroupBy](ctx, qpcgb.build, qpcgb, qpcgb.build.inters, v)
}

func (qpcgb *QueuePlaceCriterionGroupBy) sqlScan(ctx context.Context, root *QueuePlaceCriterionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(qpcgb.fns))
	for _, fn := range qpcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*qpcgb.flds)+len(qpcgb.fns))
		for _, f := range *qpcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*qpcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := qpcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// QueuePlaceCriterionSelect is the builder for selecting fields of QueuePlaceCriterion entities.
type QueuePlaceCriterionSelect struct {
	*QueuePlaceCriterionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (qpcs *QueuePlaceCriterionSelect) Aggregate(fns ...AggregateFunc) *QueuePlaceCriterionSelect {
	qpcs.fns = append(qpcs.fns, fns...)
	return qpcs
}

// Scan applies the selector query and scans the result into the given value.
func (qpcs *QueuePlaceCriterionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, qpcs.ctx, ent.OpQuerySelect)
	if err := qpcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*QueuePlaceCriterionQuery, *QueuePlaceCriterionSelect](ctx, qpcs.QueuePlaceCriterionQuery, qpcs, qpcs.inters, v)
}

func (qpcs *QueuePlaceCriterionSelect) sqlScan(ctx context.Context, root *QueuePlaceCriterionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(qpcs.fns))
	for _, fn := range qpcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*qpcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := qpcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
